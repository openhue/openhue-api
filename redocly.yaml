extends:
  - recommended

apis:
  clip@v2:
    root: ./src/main.yaml
    rules:
      no-ambiguous-paths: error

theme:
  openapi:
    schemaExpansionLevel: 2
    generateCodeSamples:
      languages:
        - lang: curl
        - lang: Java
