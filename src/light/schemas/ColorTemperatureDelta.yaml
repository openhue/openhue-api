type: object
properties:
  action:
    type: string
    enum:
      - up
      - down
      - stop
  mirek_delta:
    type: integer
    minimum: 0
    maximum: 347
    description: Mirek delta to current mirek. Clip at mirek_minimum and mirek_maximum of mirek_schema.
